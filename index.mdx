<header>
  <h1 class="text-3xl font-bold m-0">Bakuran</h1>
  Hi, I'm Steve and this is my yard.
</header>

<div class="flex flex-wrap gap-3 py-3">
{homePages.map(({ name, desc }) => (
  <comp.Card>
    <h2 className="text-xl font-bold">{name}</h2>
    {desc}
  </comp.Card>
))}
</div>
